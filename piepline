pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/sharmaanku/git-demo.git'
            }
        }

        stage('Build') {
            steps {
                sh 'echo "Pipeline Working ✅"'
            }
        }
    }
}
